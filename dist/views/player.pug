doctype html
html
    head
        title="XSCREEN PLAYER"
        link(rel="stylesheet",href='/assets/css/app.css?v=0.0.6')
        link(rel="stylesheet",href='/client/style.css')
        link(rel="stylesheet",href='/assets/css/live.css')
        script(src='http://local.xscreen.io:3000/socket.io/socket.io.js')
        meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")

    body
        div(id="overlay")

        div(id="player")
            div(id="offline")
                div(class="info vertical")
                    img(id="logo" src="/assets/images/logo.png")
            div(id="loading")
                div(class="info vertical")
                    img(id="logo" src="/assets/images/logo.png")

            div(id="remote-signal")
            div(id="count-container" v-if="count")
                p {{ counter }}

            div(id="count-overlay")
                div(id="counter-overlay-number") {{ counter }}

            div(id="print-container" class="noselect" v-on:mouseup="print" v-if="printable")
                img(class="print-logo noselect" src="/client/logo_print.png")
                img(class="print-hand noselect" src="/assets/images/hand.png")
                p(class="print-message noselect") Tik hier met één vinger<br>voor je volgnummer

        script(src='/assets/js/app.js?v=0.0.8')

